// echo "================================================ok done ================================================================================================"
// docker build -t Phishing:latest .
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "================================================ok done ================================================================================================"
                sh 'docker build -t Phishing:latest .'
            }
        }
    }
        stage('Push') {
            steps {
                echo "================================================ok done ================================================================================================"
                echo "================================================ok Pushed ================================================================================================"
}