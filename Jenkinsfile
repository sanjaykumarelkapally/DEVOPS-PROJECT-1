// echo "================================================ok done ================================================================================================"
// docker build -t Phishing:latest .
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "================================================ok done ================================================================================================"
                
            }
        }
    
        stage('Push') {
            steps {
                echo "================================================ok done ================================================================================================"
                echo "================================================ok Pushed ================================================================================================"
}
        }
}
}